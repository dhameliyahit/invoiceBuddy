# Copy this file to `.env` for local development if you need to override the API URL.
# For the single-project Vercel deployment, LEAVE THIS EMPTY so the app uses the
# same origin (/api/*) and requires no CORS configuration.
#
# Development default (used when empty in dev): http://localhost:3000
# Production default (used when empty in prod): /api (same origin)

VITE_BACKEND_URL=