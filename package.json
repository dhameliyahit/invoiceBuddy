{
  "name": "invoicebuddy",
  "version": "1.0.0",
  "private": true,
  "description": "Invoice Buddy - single Vercel deployment (React client + Express API)",
  "scripts": {
    "build": "cd client && npm install && npm run build"
  }
}
